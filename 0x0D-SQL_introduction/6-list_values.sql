--  script lists all rows of the table first_table from the database hbtn_0c_0 in a MySQL server.
-- all fields should be printed
-- the dabase name will be passed as an argument of the mysql command

SELECT * FROM first_table;
